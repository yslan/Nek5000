c-----------------------------------------------------------------------
c
c  h-refinement
c
c-----------------------------------------------------------------------
      integer*8 hmg_vertex_o((2**ldim)*lelt)
      common /hrefine_i8/ hmg_vertex_o

      integer hmg_nelt_o, hmg_nelv_o
      common /hrefine_i4/ hmg_nelt_o, hmg_nelv_o

c-----------------------------------------------------------------------
c
c  h-mg
c
c-----------------------------------------------------------------------
      integer lxc,lzc,lxyzc,lcut
      parameter(lxc=2,lzc=(lxc-1)*(ldim-2)+1)            ! crs grid size
      parameter(lxyzc=lxc**ldim)
      parameter(lcut=50)                                 ! max ncut

      integer ncut                                       ! refine into ncut segments
     $      , nblk                                       ! =ncut^d
      common /hrefinemg_i/ ncut,nblk

      real pc, pt                                        ! interp mat, and transpose
      common /hrefinemg_r/ pc(lxc*lxc,lcut),pt(lxc*lxc,lcut)

      character*3 hmg_cbco
      common /hrefinemg_c/ hmg_cbco(6,lelt)

      integer*8 se_to_gcrs(lxyzc*lelt)                   ! glo_num for crs
      common /hrefinemg_i8/ se_to_gcrs

      real hmg_crs_cmlt,hmg_crs_mask
      common /hrefinemg_crs_r/ hmg_crs_cmlt(lxyzc*lelv)
     $                       , hmg_crs_mask(lxyzc*lelv)

      integer hmg_crs_gsh                                ! gs handle for crs
      common /hrefinemg_crs_i/ hmg_crs_gsh
c-----------------------------------------------------------------------
